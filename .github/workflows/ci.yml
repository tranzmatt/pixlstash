
name: CI

permissions:
  contents: read

# A superseded run has nothing left to prove, and its runners are exactly what
# the replacement run queues behind. Cancel them on feature branches; keep every
# run on the protected branches so the record of what was green on main/develop
# stays complete.
concurrency:
  group: ci-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref_name != 'main' && github.ref_name != 'develop' }}

# `push:` used to be unfiltered, so every commit on a PR branch started the
# workflow twice — once for the push and once for the pull_request — running
# the same tests on the same SHA. That was ~2x waste when CI was three jobs;
# with the sharded matrix below it doubles the run's whole 14-job footprint
# against the concurrency budget, to queue behind a redundant copy of
# yourself. PR branches are covered by `pull_request`; the branches that get
# pushed to without a PR (protected, release-prep) and release tags are listed
# explicitly.
on:
  push:
    branches:
      - main
      - develop
      - 'release-**'
      - 'rc-prep-**'
    tags:
      - 'v*'
  pull_request:
  # Lets release-prep force the informational full-suite sweep on demand (the
  # single-process sweep job below keys off this event).
  workflow_dispatch:

env:
  # Shared pytest flags for every backend job.
  #
  # Verbosity is deliberately lower than the local command documented in
  # CLAUDE.md (`-s -vvv --log-cli-level=INFO`). `-s` disables capture, so every
  # INFO line of a ~900-test suite is streamed into the Actions log: slow to
  # write, slow to fetch, and unreadable. Capture stays ON here and the level
  # stays at INFO, so a *failing* test still reports its full captured log —
  # the debugging signal is unchanged, only the noise from passing tests is
  # gone. `-ra` lists every non-passing outcome in the trailing summary.
  #
  # `--durations=0 --durations-min=0` is the data source for the time-balanced
  # `--ci-shard` deal (tests/conftest.py). It reports every test's setup/call/
  # teardown time, which `scripts/record_test_durations.py` turns back into the
  # committed tests/ci_test_durations.json. `--durations-min=0` (rather than
  # pytest's 0.005 s default) is what makes the map COMPLETE: a test omitted
  # for being fast would be indistinguishable from a test that is new, and the
  # sharder charges an unknown test the median cost, which for a 2 ms test is
  # wrong by two orders of magnitude. The price is ~3 extra log lines per test.
  # This changes no test's behaviour and adds no runtime — it only prints.
  PYTEST_FLAGS: >-
    -q -ra --log-level=INFO --fast-captions --force-cpu
    --durations=0 --durations-min=0

jobs:
  # ---------------------------------------------------------------------------
  # Cheap, high-signal checks. Deliberately has no HF_TOKEN: it runs no pytest
  # step, so it needs no HuggingFace downloads, and the token is kept to the
  # jobs that actually do.
  # ---------------------------------------------------------------------------
  checks:
    name: checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
      - uses: ./.github/actions/setup-backend

      - name: Verify wheel includes Alembic assets
        run: |
          set -e
          python -m pip wheel . --no-deps -w /tmp/pixlstash-wheelcheck
          python - <<'PY'
          import glob
          import sys
          import zipfile

          wheel = sorted(glob.glob('/tmp/pixlstash-wheelcheck/pixlstash-*.whl'))[-1]
          required = [
              'pixlstash/alembic.ini',
              'pixlstash/migrations/env.py',
              'pixlstash/migrations/script.py.mako',
              'pixlstash/migrations/versions/0001_baseline.py',
          ]

          with zipfile.ZipFile(wheel) as archive:
              names = set(archive.namelist())

          missing = [name for name in required if name not in names]
          if missing:
              print('Missing required files in wheel:')
              for name in missing:
                  print(f' - {name}')
              sys.exit(1)

          print('Wheel Alembic asset check passed.')
          PY

      - name: Check formatting with ruff
        run: |
          ruff check pixlstash

      - name: Check backend_architecture.md is up to date
        run: |
          python scripts/render_backend_architecture.py --check

      - name: Test Frontend Utilities
        run: |
          cd frontend
          npm ci
          npm test

      # `npm run lint` is `tsc --noEmit`. `npm test` compiles with
      # tsconfig.test.json, which only covers src/ + test/; the lint pass is
      # what type-checks the main-process build as electron-builder will.
      - name: Test Electron shell
        working-directory: electron
        run: |
          npm ci
          npm run lint
          npm test

      - name: Install telemetry Worker dependencies
        working-directory: website/telemetry-worker
        run: npm ci

      - name: Test telemetry Worker
        working-directory: website/telemetry-worker
        run: npm test

      - name: Validate telemetry Worker config and D1 binding
        working-directory: website/telemetry-worker
        run: npm run check:config

  # ---------------------------------------------------------------------------
  # The single writer of the HuggingFace model cache. Every other job restores
  # it read-only (see .github/actions/setup-backend).
  #
  # Why a job and not a step in the shards: `actions/cache` writes only on a key
  # MISS, so a model missing from an entry that keeps hitting its primary key can
  # never be added to it. CLIP was missing from the 1.77 GB entry for exactly
  # that reason, so all eight shards re-fetched ~600 MB of weights on every run
  # until HuggingFace 429'd `backend shard 8` (run 31304213276). Adding the
  # missing files to the key alone would not fix it: the first run after a bust
  # would have eight runners cold-downloading the same weights at the same
  # moment, which is the same stampede. One job downloads the set once, and the
  # eight shards then start against a cache that is complete by construction.
  #
  # Cost to the blocking gate: this sits in front of the shards, so it is pure
  # critical path. On the normal warm path that is only checkout + a cache
  # restore (~1 min) — the Python install is skipped, because nothing needs to
  # be downloaded. The shards get that back by no longer downloading models.
  #
  # restore + explicit save, rather than `actions/cache`, so the write happens
  # ONLY when the prefetch succeeded. An auto-save post-step runs even after a
  # failed step, which would freeze a half-downloaded model set under the
  # primary key and recreate the bug this job exists to fix.
  # ---------------------------------------------------------------------------
  warm_models:
    name: warm model cache
    runs-on: ubuntu-latest
    # See the `backend` job for the rationale and the read-only token
    # requirement. Absent on fork PRs by design; the prefetch then runs
    # anonymously, which one sequential job is far more likely to survive than
    # eight parallel ones were.
    # Only a `push` can save this cache (see the save step), so only a `push`
    # runs the prefetch — and a run that never fetches has no use for the
    # token. Scoping it this way keeps the secret out of the environment on
    # pull-request runs, which are the ones that execute the PR's own tree via
    # `pip install .`. The shards below still carry it unconditionally: they
    # reach HuggingFace during the tests themselves.
    env:
      HF_TOKEN: ${{ github.event_name == 'push' && secrets.HF_TOKEN || '' }}
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - id: models
        uses: ./.github/actions/model-cache

      # Only needed to run the prefetch, so it is skipped whenever the cache is
      # already complete — which is every run that does not change the model set.
      - if: steps.models.outputs.hit != 'true' && github.event_name == 'push'
        uses: ./.github/actions/setup-backend

      - name: Download every model the gate loads
        if: steps.models.outputs.hit != 'true' && github.event_name == 'push'
        run: python scripts/prefetch_ci_models.py

      # Saved only from a BRANCH run, never from a pull request.
      #
      # A cache written by a `pull_request` run lands in refs/pull/N/merge and
      # is deleted with the PR, so it can never warm anything but itself — while
      # still counting against the repo's 10 GB budget for as long as the PR is
      # open. Four open PRs were holding three 1.7 GB Windows entries and a
      # duplicate 1.7 GB Linux one, with the repo at ~9 GB of 10, which is how a
      # branch-scoped entry gets LRU-evicted and the whole scheme quietly stops
      # working.
      #
      # A PR that changes the model set is therefore not fully cold: the
      # `restore-keys` prefix restores the previous set and only the delta is
      # fetched, which is what that fallback is for.
      - name: Save the model cache
        if: >-
          success() && steps.models.outputs.hit != 'true'
          && github.event_name == 'push'
        uses: ./.github/actions/model-cache
        with:
          mode: save
          key: ${{ steps.models.outputs.key }}

  # ---------------------------------------------------------------------------
  # The backend gate. Split across 8 runners instead of one job with 22
  # sequential steps.
  #
  # Eight, restored 2026-08-09 when the org moved to the GitHub Team plan and
  # the concurrency cap went from 20 concurrent jobs to 60. This number went
  # 8 -> 4 -> 8, and the middle step was correct while it stood: under a
  # 20-slot cap the budget bound ACROSS RUNS, not within one. Measured on
  # 2026-08-05 between 16:09 and 18:35, seven CI runs were in flight
  # simultaneously at 17:47; at the then-current 17 jobs per run that is ~119
  # jobs demanded against 20 slots. The cap was directly observable in run
  # 30981632678 (tag v1.9.0rc1): exactly 20 jobs started at +0-4 min and the
  # 21st, `backend shard 1`, waited 8.2 min for a slot. In run 30981629822
  # every one of the 14 test jobs was created at 06:40:28 and started between
  # 06:48:14 and 06:57:20 — an 8-17 min queue before any test ran. The same
  # saturation showed up at the other end: the trivial `build` aggregator
  # queued 13m 00s in run 31031336234 and `build-windows` 16m 47s in run
  # 31023904354. Cutting to four shards bought those slots back, at the cost of
  # wall clock on every uncontended run.
  #
  # At 60 slots that trade is gone, so the lever goes back to seconds-per-test.
  # Fourteen jobs per run leaves room for ~4 concurrent PR runs, which is what
  # this repo routinely has in flight, so eight shards can now actually spend
  # the parallelism they ask for. Each still pays its own setup-backend
  # (free-disk-space + setup-python + pip install + HuggingFace cache restore),
  # now under a minute since #796 switched the install to CPU-only torch, so
  # that fixed cost is bought out of idle capacity rather than out of the next
  # PR's slots.
  #
  # Measured basis for the resize: at four shards, 193 s setup + 2652 s pytest
  # = 45.5 min per shard, ~177 min of backend test compute per run. At eight
  # that is ~22.1 min of tests per shard. LPT over the committed map splits the
  # recorded load dead level at N=8 (679 s per shard, max/min 1.000), and the
  # balance guardrail
  # (test_ci_shards.py::test_recorded_durations_actually_balance_the_gate)
  # reads N from this matrix, so a future resize has to re-prove that rather
  # than inherit the claim. If the concurrency budget ever tightens again this
  # is still the cheapest number here to tune, and 6 is the middle setting.
  #
  # The durable fix is not the shard count, but it is not the worker fleet
  # either, which is where this comment used to point. Measured 2026-08-09, the
  # ML worker fleet costs 0.05 s per Server boot (0.46 s with it against 0.41 s
  # without), so rolling `disable_background_workers` across the suite is worth
  # ~36 s out of 5432 s. Models load lazily through `ensure_ready` in
  # pixlstash/vault.py, which only the lifespan calls, so booting a Server does
  # not load them. The real cost is that 68 of the 89 Server-using test files
  # rebuild the whole environment INSIDE each test body instead of once in a
  # shared fixture; tests/test_authentication.py shows the fixed pattern at
  # 0.43 s/test. Move those onto shared fixtures and both the shard count and
  # its wall clock stop mattering.
  #
  # The list below is the GATED set. The ~52 files that are not in it are not
  # forgotten — they are declared as DEFERRED in tests/test_ci_shards.py, and
  # that test fails if any file under tests/ is in neither list. Adding a test
  # file therefore forces an explicit decision: gate it, or record why it is
  # deferred. That is the property that matters, because the old design had no
  # such forcing function — a new file was gated only if whoever added it also
  # remembered to edit this workflow, and PR #588 is a live example of one that
  # did not.
  #
  # The seven behavioural authz/fail-closed suites — test_authz_gate_step3,
  # test_authz_gate_step4, test_authz_host_capability_16_3,
  # test_ip_locality_fail_closed, test_pictures_stream, test_scrapheap_retention
  # and test_async_import_staging — are gated here and MUST stay gated. Policy
  # *declaration* completeness has always blocked every PR
  # (test_architecture_guardrails.py::test_all_routes_declare_access_policy);
  # these are the other half, the ones that prove the AuthzGate actually
  # ENFORCES what it declares. While they were deferred their only run was the
  # non-blocking release-prep sweep, and a broken assertion in one of them sat
  # undetected for five days. tests/test_ci_shards.py::
  # test_security_suites_cannot_be_quietly_deferred fails if any of them is
  # removed from this list or parked back in DEFERRED_FROM_GATE.
  #
  # Cost of gating them, measured rather than assumed (2026-07-26): the seven
  # files are 151 tests in 182 s wall clock on a machine calibrated against this
  # runner (gate shard 6/8 = 238 s locally vs 257 s in CI, so ~1.08x). That is
  # ~3.3 min of extra test time; spread over the eight shards below it is
  # roughly +25 s per shard, not the ~20 min the block was previously assumed
  # to cost. This does NOT approach the widened release-prep budget, and it
  # must not: regular PRs stay fast.
  #
  # Why the rest of the deferred set is not simply gated too: it is not yet
  # known green, and some of it is not deterministic. Measured 2026-07-26,
  # tests/test_smart_score_invalidation.py returns a DIFFERENT pass count
  # depending on machine load — 41 passed on an idle tree, 4 failed under two
  # concurrent workloads — because its assertions race an async invalidation
  # that has not landed yet (the failures read `assert <float> is None`).
  # Collection order is deterministic (no randomising plugin), so load is the
  # variable. Do not record a specific failure count for that file, and do not
  # gate it on a loaded runner, which is its failing condition. Most of the
  # rest have simply never run as a blocking check. Gating them today would
  # turn every PR red. The flip to full discovery (`pytest tests/`, no list at
  # all) is a follow-up, blocked on getting the deferred set green AND on
  # making the load-sensitive ones deterministic — the informational sweep
  # below keeps it
  # visible in the meantime.
  #
  # `--ci-shard i/N` (implemented in tests/conftest.py) splits by *test* rather
  # than by file, which is what breaks up the authz suites and the 6-minute
  # test_server.py, and it balances the shards by RECORDED TIME
  # (longest-processing-time-first over the committed
  # tests/ci_test_durations.json). It used to deal round-robin by position:
  # that equalises test count perfectly and test time not at all, and the
  # measured result on run 30925279472 was 744 s to 1205 s across eight
  # positionally dealt shards — a 1.62x spread, with the gate waiting on shard
  # 7 while shard 6 had been idle for ~460 s. LPT over the committed map closes
  # that: max/min is 1.000 at the eight shards below, and the guardrail holds
  # any assignment under 1.05.
  #
  # The durations map is committed rather than cached because all eight shards
  # must compute the SAME partition without communicating; see the module
  # docstring of scripts/record_test_durations.py, which also regenerates it.
  # A test missing from the map keeps its old round-robin position and a
  # missing or corrupt map degrades the whole deal back to round-robin, so
  # stale data costs wall clock and never coverage.
  # tests/test_ci_shards.py asserts the matrix below and the `/N` in the
  # command agree, and that the split stays an exact partition under all of
  # those degraded inputs.
  #
  # Parallelism is deliberately across runners only, NOT pytest-xdist inside a
  # runner. xdist is installed (and is a good local `-n auto`), but two attempts
  # to validate the ML-heavy half of the suite under `-n auto` ended with every
  # worker aborting during teardown — "terminate called without an active
  # exception", a C++ thread-pool destructor firing while onnxruntime/torch
  # state was being released in conftest's pytest_sessionfinish. That is a
  # crash with no failing test attached to it, which is the worst kind of CI
  # flake to debug. Sharding across jobs buys the same wall clock with one
  # single-threaded pytest per runner and no new native-teardown concurrency.
  # Revisit `-n 2` only with a green measured run behind it.
  # ---------------------------------------------------------------------------
  # The Windows half of the same scheme, as a SEPARATE job rather than a matrix
  # leg of `warm_models`: `backend` does `needs: warm_models`, and `needs:` on a
  # matrix job waits for every leg, so a matrix would put a Windows runner on
  # the Linux gate's critical path for nothing.
  #
  # Nothing was writing a Windows entry to a branch scope before this. The four
  # `backend_windows` shards each carried an inline read/write `actions/cache`,
  # which on a `pull_request` run writes into refs/pull/N/merge — a scope that
  # dies with the PR. Every PR therefore paid four cold ~1.7 GB downloads that
  # hit HuggingFace in parallel and 429'd, which is exactly the stampede
  # `warm_models` above exists to prevent, one platform later. Measured on run
  # 31418525447: `likeness pipeline did not settle in time` behind five retries
  # of `HTTP Error 429` per file.
  # ---------------------------------------------------------------------------
  warm_models_windows:
    name: warm model cache (Windows)
    runs-on: windows-latest
    # Only a `push` can save this cache (see the save step), so only a `push`
    # runs the prefetch — and a run that never fetches has no use for the
    # token. Scoping it this way keeps the secret out of the environment on
    # pull-request runs, which are the ones that execute the PR's own tree via
    # `pip install .`. The shards below still carry it unconditionally: they
    # reach HuggingFace during the tests themselves.
    env:
      HF_TOKEN: ${{ github.event_name == 'push' && secrets.HF_TOKEN || '' }}
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - id: models
        uses: ./.github/actions/model-cache

      # `setup-backend` caches pip at ~/.cache/pip, which is not where pip
      # caches on Windows, so it is Linux-only and this is its counterpart.
      # Deliberately no pip cache: this job installs only to run the prefetch,
      # and a second 0.4 GB Windows entry is not worth it against the 10 GB
      # repo budget this change is trying to get back under.
      - name: Set up Python
        if: steps.models.outputs.hit != 'true' && github.event_name == 'push'
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: '3.12'

      - name: Install dependencies
        if: steps.models.outputs.hit != 'true' && github.event_name == 'push'
        run: |
          python -m pip install --upgrade pip
          pip install .[test,dev]

      - name: Download every model the Windows shards load
        if: steps.models.outputs.hit != 'true' && github.event_name == 'push'
        run: python scripts/prefetch_ci_models.py

      - name: Save the model cache
        if: >-
          success() && steps.models.outputs.hit != 'true'
          && github.event_name == 'push'
        uses: ./.github/actions/model-cache
        with:
          mode: save
          key: ${{ steps.models.outputs.key }}

  # ---------------------------------------------------------------------------
  backend:
    name: backend shard ${{ matrix.shard }}
    runs-on: ubuntu-latest
    # The shards only read the model cache; `warm_models` is what fills it.
    # Waiting on it is the serialisation that stops eight runners cold-fetching
    # the same weights at once.
    #
    # `needs:` alone would turn a failed warming job into eight SKIPPED shards,
    # not eight failed ones — the skipped-is-not-failed ambiguity this repo
    # already had to design against once (see the removed `build` aggregator
    # note below). Warming is an optimisation, never a correctness gate, so
    # `!cancelled()` keeps the ordering and drops the veto: if warming breaks,
    # the shards still run, still report red or green on their own merits, and
    # `warm model cache` is the red check that says why they were slow.
    needs: warm_models
    if: ${{ !cancelled() }}
    strategy:
      # Every shard must report: one shard failing tells you nothing about the
      # other seven, and a partial picture costs another full round trip.
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4, 5, 6, 7, 8]
    # HF_TOKEN lifts HuggingFace's anonymous-download rate limit, which was
    # throttling cold model fetches past the import tests' 10s timeout. Use a
    # READ-ONLY, public-scope fine-grained token: that minimal scope is the
    # compensating control for exposing it job-wide. Set job-wide rather than
    # per-step so a newly added pytest step can't silently reintroduce the
    # flake. Absent on fork PRs by design — GitHub withholds secrets from
    # forked-PR runs, which fall back to unauthenticated downloads. Do NOT
    # switch those to pull_request_target.
    env:
      HF_TOKEN: ${{ secrets.HF_TOKEN }}
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
      - uses: ./.github/actions/setup-backend

      - name: Backend tests (shard ${{ matrix.shard }} of 8)
        env:
          CI_SHARD: ${{ matrix.shard }}
        run: >-
          python -m pytest $PYTEST_FLAGS --ci-shard "$CI_SHARD/8"
          tests/multi_project_authz/test_generic_field_reader_allowlist.py
          tests/multi_project_authz/test_multi_project_membership_authz.py
          tests/test_adapter_attachment.py
          tests/test_adapter_header.py
          tests/test_aitoolkit_run.py
          tests/test_anomaly_region_scope.py
          tests/test_app_permission_repair.py
          tests/test_architecture_guardrails.py
          tests/test_async_import_staging.py
          tests/test_auth_read_path.py
          tests/test_authentication.py
          tests/test_authz_gate_step3.py
          tests/test_authz_gate_step4.py
          tests/test_authz_host_capability_16_3.py
          tests/test_authz_library_pin.py
          tests/test_backend_order_stability.py
          tests/test_batch_character_likeness_scope.py
          tests/test_builtin_models.py
          tests/test_character_face_assignment.py
          tests/test_characters_api.py
          tests/test_checkpoint_hash_finder.py
          tests/test_ci_shards.py
          tests/test_cli_documentation.py
          tests/test_clip_text_embedding_similarity.py
          tests/test_comfyui_api_prompt_extraction.py
          tests/test_comfyui_pixlstash_saver.py
          tests/test_comfyui_recipe_preflight.py
          tests/test_comfyui_recipe_route.py
          tests/test_comfyui_stack_filter.py
          tests/test_comfyui_submit_prompt.py
          tests/test_comfyui_workflow_extraction.py
          tests/test_database_engine_config.py
          tests/test_dedup_mixed_stacks.py
          tests/test_dedup_sweep_api.py
          tests/test_dedup_sweep_service.py
          tests/test_dedup_tier_service.py
          tests/test_dedup_tiers_api.py
          tests/test_dedup_verdict_service.py
          tests/test_description_cancel_blanking.py
          tests/test_detections_scope.py
          tests/test_docker_demo_hub_rewrite.py
          tests/test_export_api.py
          tests/test_face_score_aggregation.py
          tests/test_facial_features_worker.py
          tests/test_find_orphaned_files.py
          tests/test_finder_query_cost.py
          tests/test_florence.py
          tests/test_florence_model_variant.py
          tests/test_folder_structure_commit.py
          tests/test_folder_structure_read.py
          tests/test_hub_bootstrap.py
          tests/test_hub_engine.py
          tests/test_hub_model_shelf_schema.py
          tests/test_hub_registry.py
          tests/test_identity_storage_guardrail.py
          tests/test_image_embedding_worker.py
          tests/test_import_scrapheap_match.py
          tests/test_impossible_clear_authz.py
          tests/test_index_install_type.py
          tests/test_inline_rotate.py
          tests/test_insightface_relocation.py
          tests/test_install_type_buckets.py
          tests/test_invalid_image_endless_retry.py
          tests/test_ip_locality_fail_closed.py
          tests/test_keep_cover_only.py
          tests/test_known_base_models.py
          tests/test_label_ledger.py
          tests/test_libraries_routes.py
          tests/test_library_backup.py
          tests/test_library_generation_coordinator.py
          tests/test_library_insights.py
          tests/test_library_layout.py
          tests/test_library_restore_cli.py
          tests/test_library_settings.py
          tests/test_library_switch.py
          tests/test_likeness_and_face_search.py
          tests/test_likeness_worker.py
          tests/test_managed_model_store.py
          tests/test_many_to_many_face_data.py
          tests/test_metadata_hash.py
          tests/test_metadata_hash_batching.py
          tests/test_migrations.py
          tests/test_model_file_classification.py
          tests/test_model_folder_scanner.py
          tests/test_model_icons.py
          tests/test_model_move.py
          tests/test_model_run_import.py
          tests/test_model_shelf_api.py
          tests/test_model_shelf_fixtures.py
          tests/test_model_shelf_library_fixture.py
          tests/test_model_shelf_scan_invariants.py
          tests/test_model_unload_race.py
          tests/test_openapi_response_schemas.py
          tests/test_operation_log.py
          tests/test_orientation.py
          tests/test_path_containment.py
          tests/test_pending_score_invalidation.py
          tests/test_picture_is_video.py
          tests/test_picture_mutation_scope.py
          tests/test_picture_plugins.py
          tests/test_picture_set_locking.py
          tests/test_picture_sets.py
          tests/test_picture_sets_query_cost.py
          tests/test_picture_stack_utils.py
          tests/test_pictures_stream.py
          tests/test_pipeline_stage_contracts.py
          tests/test_pipeline_throughput_verification.py
          tests/test_plugin_create.py
          tests/test_plugin_install.py
          tests/test_plugin_lifecycle_gaps.py
          tests/test_project_membership_service.py
          tests/test_projects_api.py
          tests/test_rate_limiter.py
          tests/test_read_token_security.py
          tests/test_reference_folder_moves.py
          tests/test_reference_folder_watcher.py
          tests/test_repair_copied_face_rows.py
          tests/test_restore.py
          tests/test_reviews_api.py
          tests/test_scope_table.py
          tests/test_score_agreement_stats.py
          tests/test_scrapheap_retention.py
          tests/test_security_supported_versions.py
          tests/test_server.py
          tests/test_share_link_library_pin.py
          tests/test_sidebar_counts_query_cost.py
          tests/test_smart_score_consistency.py
          tests/test_snapshots.py
          tests/test_snapshots_auth.py
          tests/test_ssl_generation.py
          tests/test_stack_detection.py
          tests/test_stack_leader_scope.py
          tests/test_stack_state_filter.py
          tests/test_stacks_api.py
          tests/test_stacks_membership.py
          tests/test_startup_permissions.py
          tests/test_system_utils.py
          tests/test_tag_prediction_backfill.py
          tests/test_tag_prediction_scope.py
          tests/test_tag_task.py
          tests/test_tag_worker.py
          tests/test_tagger_plugin_registry.py
          tests/test_tagger_user_plugins.py
          tests/test_thumbnail_cache_headers.py
          tests/test_thumbnail_location.py
          tests/test_token_identity.py
          tests/test_telemetry_install_id.py
          tests/test_telemetry_install_id_authz.py
          tests/test_telemetry_sender.py
          tests/test_views_links.py
          tests/test_vram_batch_budget.py
          tests/test_vram_oom_retry.py
          tests/test_watch_folder_worker.py
          tests/test_websocket_auth.py
          tests/test_workers_api.py
          tests/test_work_planner.py
          tests/test_workflow_library.py
          tests/test_ws_broadcaster.py

  # ---------------------------------------------------------------------------
  # RELEASE-PREP ONLY — informational ordering sweep, sharded by contiguous
  # block.
  #
  # This job's value is the ORDER, not coverage: the gate deals its tests
  # round-robin, which is exactly the thing that could hide (or cause) an
  # order- or shard-dependence. This job is the control for that.
  #
  # Note it is ALSO the only run for the files in DEFERRED_FROM_GATE
  # (tests/test_ci_shards.py) — a substantial slice of tests/. Those are
  # therefore covered only here, only during release prep, and only
  # informationally. That is a known gap, not a design goal: an earlier version
  # of this comment claimed "the whole suite now blocks on every run", which was
  # never true and contradicted the deferral list in the same tree. The
  # behavioural authz/fail-closed suites used to be in that gap and are now
  # gated; prefer moving the remaining files onto the gate over relying on this
  # sweep. It keeps the `continue-on-error` semantics — a red here is a triage
  # signal, not a gate failure — and the release-prep trigger.
  #
  # It used to be one process running `pytest tests/` end to end: the perfect
  # control, at ~40-50 min, on the critical path of every release prep. It is
  # now split across a matrix, which necessarily weakens the control, so the
  # split was chosen to give up as little of it as possible:
  #
  #  * `--ci-block-shard i/N` (tests/conftest.py) gives shard i the i-th
  #    CONTIGUOUS slice of the canonical collection order. Relative order is
  #    preserved inside every shard, so any order dependence between two tests
  #    still fails unless the pair happens to straddle one of the N-1 block
  #    boundaries. Weaker than one process; far stronger than round-robin.
  #  * NOT `--ci-shard`. Reusing the gate's round-robin here would shard the
  #    shard-dependence detector with the same dealing algorithm it exists to
  #    audit — the one arrangement guaranteed to prove nothing.
  #  * Not "contiguous blocks of whole files" either. It preserves a little
  #    more (boundaries always land between files), but it cannot split a slow
  #    single file such as the 6-min test_server.py, so the slowest shard would
  #    still be most of the wall clock. Test-level blocks lose at most N-1
  #    adjacency points to buy a real split. (This bullet used to cite a
  #    "~20-min authz block" as the other example; the seven behavioural authz
  #    files were measured at 182 s total on 2026-07-26 and are now gated, so
  #    test_server.py is the real case.)
  #  * Not "keep one full-order job, but only on refs/tags/v*". That preserves
  #    the control perfectly and loses it exactly where it is wanted: an
  #    ordering bug found at tag time is found after the release candidate has
  #    already been signed off, and it puts a 50-min serial job on the tag
  #    build's critical path.
  #
  # N=6. Two reasons, and the second is the binding one. (1) Concurrency: the
  # rest of this workflow queues 14 jobs (checks 1 + backend 8 + e2e 1 +
  # backend_windows 4) against the Team plan's
  # 60-concurrent-job budget, so 6 fits without the sweep queueing behind the
  # gate it is supposed to run alongside — and unlike the gate it runs only
  # during release prep, when there is rarely a queue of feature-branch runs
  # competing. (2) Diminishing returns: the fixed per-runner cost here (disk
  # reclaim + pip install + HuggingFace cache restore) is ~10 min and does not
  # shard, so past ~6 shards each extra runner buys a minute or two of test
  # time and pays a full setup for it. Expected wall clock ~16-20 min: ~10 min
  # setup plus the slowest block. Blocks are equal in test COUNT, not in test
  # TIME — that imbalance is the price of preserving order.
  #
  # The per-shard check name is NOT stable across a resize, by design: this job
  # is informational and must never be a required check. Nothing is a required
  # check in this repo — see the removal note above `e2e`.
  #
  # The old "1.7.0 headline API surface" step is gone: its five files
  # (test_tag_health_api, test_reviews_api, test_tag_predictions_api,
  # test_tag_suggestions_api, test_tagger_runs_api) are now collected and
  # blocking on every run, which is strictly stronger than blocking only during
  # release prep.
  # ---------------------------------------------------------------------------
  backend_release_sweep:
    name: backend-full block ${{ matrix.shard }} (informational, release-prep only)
    if: ${{ !cancelled() && (startsWith(github.head_ref || github.ref_name, 'rc-prep') || startsWith(github.head_ref || github.ref_name, 'release') || startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch') }}
    runs-on: ubuntu-latest
    # Same reason as the gate: read the model cache, never fill it, and never
    # let a failed warming job silently skip the sweep instead of running it.
    needs: warm_models
    strategy:
      # Every block must report, for the same reason as the gate: one red block
      # says nothing about the other five, and release prep cannot afford
      # another full round trip to find out.
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4, 5, 6]
    # See the `backend` job for the rationale and the read-only token requirement.
    env:
      HF_TOKEN: ${{ secrets.HF_TOKEN }}
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
      - uses: ./.github/actions/setup-backend

      - name: Full suite in collection order (block ${{ matrix.shard }} of 6)
        continue-on-error: true
        env:
          CI_SHARD: ${{ matrix.shard }}
        run: >-
          python -m pytest $PYTEST_FLAGS --ci-block-shard "$CI_SHARD/6"
          tests/

  # ---------------------------------------------------------------------------
  # REMOVED 2026-08-09 (#796): the `build` and `build-windows` aggregator jobs.
  # They existed only to give branch protection one stable check name per OS,
  # and there is no branch protection to serve: `develop` has none, `main` is
  # protected with no required_status_checks at all, and merges are a human
  # judgement call by design. `build` also cost real tail latency — it needed
  # every Linux job, so it queued for its own runner after the gate finished
  # (up to ~16 min for a 3-second job).
  #
  # They carried one property beyond aggregation (CSO condition F2): `skipped`
  # counted as a FAILURE for `checks`/`backend`, so a `paths:`/`if:` filter
  # could not leave a green required check that ran zero tests (`skipped` was a
  # pass only for `backend_release_sweep`, which is skipped outside release
  # prep by design). That is moot while nothing is required. Anything that
  # reintroduces a required status check, or adds a `paths:`/`if:` filter to
  # the gate, MUST restore the skipped-is-failure guard with it.
  # ---------------------------------------------------------------------------

  e2e:
    # Playwright UI tests drive the real SPA against a backend booted on a
    # throwaway copy of the committed test-data/ fixture (browse-only: workers
    # disabled, no ML models needed). The fixture is committed release input:
    # its absence is a coverage failure, never permission to skip the lane.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Require committed e2e fixture
        id: fixture
        run: |
          set -euo pipefail
          git ls-files --error-unmatch test-data/images/vault.db
          test -f test-data/images/vault.db
          echo "present=true" >> "$GITHUB_OUTPUT"

      - name: Set up Python
        if: steps.fixture.outputs.present == 'true'
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: '3.12'

      - name: Cache pip packages
        if: steps.fixture.outputs.present == 'true'
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5.1.0
        with:
          path: ~/.cache/pip
          # Deliberately the SAME key as the setup-backend composite, so this
          # job and the backend shards share one pip download cache instead of
          # keeping two near-identical copies per ref.
          #
          # The trade-off is real and was decided on a measurement, not a
          # guess. This job installs `pip install .` while the composite
          # installs `.[test,dev]`, and actions/cache only writes on a key
          # miss — so whichever job races to the miss first fixes the contents
          # for both, and the loser re-downloads its delta on every run
          # without ever getting a miss to re-save. That delta was the whole
          # question, so it was measured: a separate `pip-e2e-cpu-` namespace
          # saved 494 MB against the composite's 507 MB. The test/dev extras
          # are ~13 MB of small pure-Python wheels; that is the entire cost of
          # losing the race. Against it, a second namespace costs ~494 MB on
          # every active ref, in a repo already evicting entries against
          # GitHub's 10 GB limit — which is what makes runs miss cache and
          # install cold in the first place. 13 MB per run beats 494 MB per
          # ref. Contents cannot diverge dangerously either way: pip's HTTP
          # cache is keyed by URL, so a subset or superset just means pip
          # downloads whatever is not there.
          #
          # `-cpu-` is not optional in either design: restore-keys is a prefix
          # match, so the old `pip-` prefix would restore the 2.79 GB CUDA-fat
          # cache and re-save those nvidia wheels forever.
          key: pip-cpu-${{ runner.os }}-py3.12-${{ hashFiles('setup.py', 'requirements.txt') }}
          restore-keys: pip-cpu-${{ runner.os }}-py3.12-

      - name: Install backend
        if: steps.fixture.outputs.present == 'true'
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          # CPU-only torch, CI ONLY — same treatment the setup-backend
          # composite got, which this job cannot share because it has its own
          # inline Python setup. This runner only drives Chromium against a
          # browse-only backend (workers disabled, no ML models), so the CUDA
          # payload PyPI's torch drags in (nvidia-*-cu13, triton, ~2.5 GB
          # unpacked) is pure cost: it was most of the 92 s install and the
          # reason this job's pip cache was 2.79 GB.
          # The specifiers are pyproject.toml's own, so CI resolves exactly
          # the version it resolves today and just takes the +cpu build of it;
          # `pip install .` below then finds the requirement already satisfied
          # and leaves it alone. --index-url, not --extra-index-url: it
          # confines this one invocation to PyTorch's own index instead of
          # letting a second index shadow PyPI names. Nothing a user installs
          # changes — `pip install -e .` still resolves the GPU build.
          pip install --index-url https://download.pytorch.org/whl/cpu \
            "torch~=2.12" "torchvision~=0.27"
          pip install .
          df -h /
          # Fail loudly if a pin change ever lets PyPI's CUDA torch win again.
          # sed, not `grep -q`: under `pipefail` an early-exiting grep SIGPIPEs
          # pip and the guard fires on its own plumbing instead of the version.
          torch_version="$(pip show torch | sed -n 's/^Version: //p')"
          case "$torch_version" in
            *+cpu) echo "torch $torch_version" ;;
            *)
              echo "::error::torch is $torch_version, not a +cpu build; the CUDA wheels are back."
              exit 1
              ;;
          esac

      - name: Set up Node
        if: steps.fixture.outputs.present == 'true'
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.5.0
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend deps and Chromium
        if: steps.fixture.outputs.present == 'true'
        run: |
          cd frontend
          npm ci
          # This job used to reclaim runner disk with jlumbroso/free-disk-space
          # (88 s) before installing anything. That step is gone, so the
          # headroom is measured instead of assumed - and measured BEFORE
          # Chromium lands, because that is the download that runs out of it.
          # A bare `df` after the fact prints on the green runs and is missing
          # on the one that matters: the job dies inside `playwright install`
          # with an ENOSPC that reads as a Playwright bug. A warning, not a
          # failure: 4 GiB is a heuristic, and a heuristic must not be able to
          # fail a build on its own. If this fires, put the reclaim step back.
          avail_kb="$(df --output=avail -k / | tail -1)"
          if [ "$avail_kb" -lt 4194304 ]; then
            echo "::warning title=Low runner disk::Only $((avail_kb / 1024)) MiB free on / before installing Chromium. If this job failed with ENOSPC, that is why; restore the jlumbroso/free-disk-space step."
          fi
          npx playwright install --with-deps chromium
          df -h /

      - name: Run Playwright e2e
        if: steps.fixture.outputs.present == 'true'
        run: |
          cd frontend
          npx playwright test

      - name: Upload Playwright report
        if: ${{ always() && steps.fixture.outputs.present == 'true' }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: playwright-report
          path: frontend/playwright-report
          retention-days: 7

  # ---------------------------------------------------------------------------
  # Windows only validates OS-sensitive behaviour (file/path/process handling,
  # DB file swap, server startup, image libs). The ML-inference tests are
  # OS-independent — PyTorch/CLIP/InsightFace behave the same on Windows — so
  # they run on Linux only to keep this job fast. That subset is therefore a
  # deliberate scope choice, not a gate, which is why this job keeps an explicit
  # file list while the Linux gate does not: the completeness guarantee lives on
  # Linux, and a file missing here costs Windows-specific breadth, not coverage.
  #
  # The list is sharded with the same `--ci-shard` mechanism, without xdist:
  # `test_server.py` alone was 13 min of the 30, and splitting by test (rather
  # than by file) is what actually breaks that up. Job-level sharding adds no
  # intra-process concurrency, so it introduces no new flake surface on the
  # platform that is hardest to debug remotely.
  #
  # Four shards, and this one does NOT track the Linux gate's count. Windows
  # pays the worst fixed cost per shard of any job here: measured on run
  # 31296192128 (`backend-windows shard 2`), checkout + Node + Python + caches
  # is 41 s and `pip install .[test,dev]` another 160 s, so ~3.6 min of every
  # shard is spent before a test runs. That is what caps this at 4 rather than
  # the gate's 8 — ~35 min of Windows test compute over 4 shards is 8.75 min of
  # tests + 3.6 min setup = ~12 min per shard, and doubling again would buy ~4
  # min of test time per runner while paying 3.6 min of setup for it. Measured
  # after the resize (run 31302551327): 12.3 / 13.2 / 13.7 / 13.9 min.
  #
  # Note that #801's CPU-only-torch install fix does NOT apply here: the
  # `nvidia-*-cu13` / `cuda-toolkit` wheels are gated `platform_system ==
  # "Linux"`, and the caches agree (pip-Windows-py3.12 0.37 GB against
  # pip-Linux-py3.12 2.79 GB). There is no CUDA payload on this runner to
  # remove, so the 160 s install above is the floor.
  #
  # It was 2 until 2026-08-09. Half of that argument was the 20-slot
  # concurrency cap; the move to the GitHub Team plan (60 concurrent jobs)
  # removed that half, and only the fixed-cost arithmetic above still holds.
  # The resize saves nothing today — that same run had this job at 21.2 min
  # against the Linux gate's ~21.3 min, i.e. tied. It is pre-emptive: the
  # in-flight test-fixture work takes Linux to roughly 16 min, at which point
  # an unshrunk Windows job becomes the critical path on its own.
  #
  # This is also a deliberate SUBSET rather than a gate, so it is the cheaper
  # place to give up parallelism: the completeness guarantee lives on Linux.
  # ---------------------------------------------------------------------------
  backend_windows:
    name: backend-windows shard ${{ matrix.shard }}
    # Same reason `backend` waits for `warm_models`: without the gate all four
    # shards start cold together and re-run the very stampede the warm job
    # exists to absorb. On the warm path this costs the shards a checkout and a
    # cache restore they were doing anyway.
    needs: warm_models_windows
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4]
    # See the `backend` job for the rationale and the read-only token requirement.
    env:
      HF_TOKEN: ${{ secrets.HF_TOKEN }}
      # This job intermittently exits 139 SECONDS AFTER pytest prints a fully
      # green summary, with no traceback anywhere in the log. pytest's bundled
      # faulthandler plugin tears itself down in pytest_unconfigure, so it is
      # already gone by then; that places the crash in Py_FinalizeEx. The env
      # var enables faulthandler at interpreter start-up instead, where nothing
      # can unregister it, so the next occurrence names the thread and frame.
      # Costs nothing: it only installs a signal handler, and prints only if
      # the process actually dies on a fatal signal.
      PYTHONFAULTHANDLER: 1
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      # The wheel build (pip install . below) runs `npm ci` via setup.py. Set up
      # Node with an npm cache so that step pulls tarballs from the warm cache
      # instead of cold over the network on every run — the main source of the
      # ECONNRESET flake here. Matches electron.yml / windows-installer.yml.
      - name: Set up Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.5.0
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: '3.12'

      - name: Cache pip packages
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5.1.0
        with:
          path: ~\AppData\Local\pip\Cache
          key: pip-${{ runner.os }}-py3.12-${{ hashFiles('setup.py', 'requirements.txt') }}
          restore-keys: pip-${{ runner.os }}-py3.12-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install .[test,dev]

      # Restore-only, through the same action every Linux job uses. This was an
      # inline read/write `actions/cache` with its own paths and a key hashing
      # three of the six files the shared one hashes — two definitions that
      # could never share an entry, and the writing half is what filled the
      # repo's cache budget with per-PR copies. `warm_models (windows-latest)`
      # is now the single writer, exactly as on Linux.
      - name: Restore the HuggingFace model cache
        uses: ./.github/actions/model-cache

      - name: OS-sensitive backend tests (shard ${{ matrix.shard }} of 4)
        shell: bash
        env:
          CI_SHARD: ${{ matrix.shard }}
        run: >-
          python -m pytest $PYTEST_FLAGS --ci-shard "$CI_SHARD/4"
          tests/test_migrations.py
          tests/test_snapshots.py
          tests/test_restore.py
          tests/test_snapshots_auth.py
          tests/test_metadata_hash.py
          tests/test_architecture_guardrails.py
          tests/multi_project_authz/test_multi_project_membership_authz.py
          tests/test_openapi_response_schemas.py
          tests/test_find_orphaned_files.py
          tests/test_library_layout.py
          tests/test_server.py
          tests/test_authentication.py
          tests/test_rate_limiter.py
          tests/test_picture_sets.py
          tests/test_picture_stack_utils.py
          tests/test_picture_plugins.py
          tests/test_comfyui_api_prompt_extraction.py
          tests/test_comfyui_recipe_preflight.py
          tests/test_comfyui_recipe_route.py
          tests/test_comfyui_submit_prompt.py
          tests/test_comfyui_workflow_extraction.py
          tests/test_watch_folder_worker.py
          tests/test_work_planner.py

      # `PYTHONFAULTHANDLER` above did not name it, and that is itself the
      # finding: pytest's faulthandler plugin restores the handler in its
      # `pytest_unconfigure` whenever the env var enabled it (verified in
      # `_pytest/faulthandler.py`), so it IS armed all the way into
      # `Py_FinalizeEx` — and six of these crashes have still printed nothing.
      # A handler that is live and silent puts the fault beyond CPython's reach
      # entirely: past `_PyFaulthandler_Fini`, in native teardown. Windows logs
      # one layer below that, an Application Error record naming the faulting
      # module, its version and the fault offset — the DLL this has never been
      # able to name.
      #
      # Read the event rather than shipping a minidump: artifacts on a public
      # repository are downloadable by anyone, and dumped process memory reaches
      # the environment block, which is where this job's `HF_TOKEN` lives. An
      # event record carries module names and offsets and no memory at all.
      - name: Name the faulting module
        if: failure()
        shell: pwsh
        run: |
          $events = Get-WinEvent -MaxEvents 5 -ErrorAction SilentlyContinue -FilterHashtable @{
            LogName      = 'Application'
            ProviderName = 'Application Error', 'Windows Error Reporting'
          }
          if (-not $events) {
            Write-Host 'No Application Error record: this failure was not a native crash.'
            exit 0
          }
          foreach ($event in $events) {
            Write-Host "--- $($event.TimeCreated.ToString('o')) [$($event.Id)] $($event.ProviderName)"
            Write-Host $event.Message
          }

  # The `build-windows` aggregator over this job was removed with `build` in
  # #796 — see the note where `build` used to be, above `e2e`.
